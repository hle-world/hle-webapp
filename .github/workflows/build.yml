name: Build & Release

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: "Version tag (e.g. v1.19.0)"
        required: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      # Build docker/standalone variant (default mode)
      - name: Build frontend (docker mode)
        working-directory: frontend
        env:
          VITE_APP_MODE: docker
        run: npm run build -- --outDir dist-docker

      # Build ha-addon variant (HA-specific UI enabled)
      - name: Build frontend (ha-addon mode)
        working-directory: frontend
        env:
          VITE_APP_MODE: ha-addon
        run: npm run build -- --outDir dist-ha-addon

      - name: Determine version
        id: version
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          INPUT_VERSION: ${{ github.event.inputs.version }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          if [ "$EVENT_NAME" = "release" ]; then
            echo "tag=${RELEASE_TAG}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=${INPUT_VERSION}" >> "$GITHUB_OUTPUT"
          fi

      - name: Package hle-webapp tarball (docker frontend)
        env:
          VERSION: ${{ steps.version.outputs.tag }}
        run: |
          DIRNAME="hle-webapp-${VERSION#v}"
          mkdir -p "${DIRNAME}/backend/static"
          cp -r backend/*.py backend/__init__.py "${DIRNAME}/backend/"
          cp -r frontend/dist-docker/* "${DIRNAME}/backend/static/"
          cp run.sh "${DIRNAME}/"
          cp requirements.txt "${DIRNAME}/"
          chmod +x "${DIRNAME}/run.sh"
          tar czf "hle-webapp-${VERSION#v}.tar.gz" "${DIRNAME}"

      - name: Package ha-addon frontend artifact
        env:
          VERSION: ${{ steps.version.outputs.tag }}
        run: tar czf "hle-frontend-ha-addon-${VERSION#v}.tar.gz" -C frontend/dist-ha-addon .

      - name: Package docker frontend artifact
        env:
          VERSION: ${{ steps.version.outputs.tag }}
        run: tar czf "hle-frontend-docker-${VERSION#v}.tar.gz" -C frontend/dist-docker .

      - name: Upload release assets
        if: github.event_name == 'release'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          gh release upload "${RELEASE_TAG}" \
            "hle-webapp-${RELEASE_TAG#v}.tar.gz" \
            "hle-frontend-ha-addon-${RELEASE_TAG#v}.tar.gz" \
            "hle-frontend-docker-${RELEASE_TAG#v}.tar.gz" \
            --clobber

      - name: Upload artifacts (manual builds)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: hle-webapp-${{ steps.version.outputs.tag }}
          path: "*.tar.gz"

      # Dispatch to ha-addon and hle-docker so they sync the new pre-built frontend
      - name: Dispatch webapp-release to distribution repos
        if: github.event_name == 'release'
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          VERSION="${RELEASE_TAG#v}"
          for repo in ha-addon hle-docker; do
            curl -s -X POST \
              -H "Authorization: Bearer ${GH_TOKEN}" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/repos/hle-world/${repo}/dispatches" \
              -d "{\"event_type\":\"webapp-release\",\"client_payload\":{\"version\":\"${VERSION}\"}}"
          done
