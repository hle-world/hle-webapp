name: Sync HLE Docker Changes

on:
  repository_dispatch:
    types: [hle-docker-changes]

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Sync frontend and backend from hle-docker
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
          PR_TITLE: ${{ github.event.client_payload.pr_title }}
          PR_NUM: ${{ github.event.client_payload.pr_number }}
        run: |
          # Sync shared backend files
          for file in models.py hle_api.py tunnel_manager.py __init__.py; do
            curl -sL "https://raw.githubusercontent.com/hle-world/hle-docker/main/backend/${file}" -o "backend/${file}"
          done

          # Sync frontend source
          rm -rf frontend/src frontend/public
          gh api repos/hle-world/hle-docker/git/trees/main?recursive=1 \
            --jq '.tree[] | select(.path | startswith("frontend/src/") or startswith("frontend/public/")) | .path' \
            | while read -r fpath; do
                mkdir -p "$(dirname "${fpath}")"
                curl -sL "https://raw.githubusercontent.com/hle-world/hle-docker/main/${fpath}" -o "${fpath}"
              done

          # Copy frontend config files
          for f in package.json package-lock.json tsconfig.json tsconfig.app.json tsconfig.node.json vite.config.ts index.html; do
            curl -sL "https://raw.githubusercontent.com/hle-world/hle-docker/main/frontend/${f}" -o "frontend/${f}" 2>/dev/null || true
          done

          if git diff --quiet && [ -z "$(git ls-files --others --exclude-standard)" ]; then
            echo "No changes to sync."
            exit 0
          fi

          BRANCH="chore/sync-hle-docker-pr-${PR_NUM}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "${BRANCH}"
          git add -A
          git commit -m "chore: sync changes from hle-docker (PR #${PR_NUM})"
          git push -u origin "${BRANCH}"
          gh pr create \
            --title "chore: sync from hle-docker — ${PR_TITLE}" \
            --body "Automated sync from hle-docker PR #${PR_NUM}." \
            --base main
