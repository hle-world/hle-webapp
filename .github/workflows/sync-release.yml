name: Sync Client Release

on:
  repository_dispatch:
    types: [hle-client-release]

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Update client version
        env:
          CLIENT_VERSION: ${{ github.event.client_payload.version }}
        run: |
          # Strip v prefix if present (dispatch payload uses tag name e.g. v2604.1)
          CLEAN_VERSION="${CLIENT_VERSION#v}"

          # Every field of a repository_dispatch payload is attacker-chosen for
          # anyone able to send one, and this value goes on to be a sed
          # replacement, a branch name, a commit message and a PR title. It is
          # only ever a CalVer tag, so say so: anything else stops here rather
          # than reaching one of those.
          case "${CLEAN_VERSION}" in
            [0-9][0-9][0-9][0-9].[0-9]|[0-9][0-9][0-9][0-9].[0-9][0-9]) ;;
            *)
              echo "::error::Refusing dispatch: version '${CLIENT_VERSION}' is not CalVer YYMM.RELEASE"
              exit 1
              ;;
          esac

          sed -i "s/hle-client==[0-9v.]*/hle-client==${CLEAN_VERSION}/" requirements.txt

      - name: Create PR
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
          CLIENT_VERSION: ${{ github.event.client_payload.version }}
        run: |
          BRANCH="chore/sync-client-${CLIENT_VERSION}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "${BRANCH}"
          git add requirements.txt
          git commit -m "chore: bump hle-client to ${CLIENT_VERSION}"
          git push -u origin "${BRANCH}"
          PR_URL=$(gh pr create \
            --title "chore: bump hle-client to ${CLIENT_VERSION}" \
            --body "Automated sync from hle-client release v${CLIENT_VERSION}. Auto-merge enabled — will merge when CI passes." \
            --base main)
          echo "Opened: ${PR_URL}"
          gh pr merge "${PR_URL}" --auto --squash || \
            echo "::warning::Could not enable auto-merge (check repo settings)"
