name: Test

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  backend:
    name: Backend compile + lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend deps
        # Retry: a fresh hle-client release may not be installable from *this*
        # runner yet when the sync PR's CI runs. Without this the pin bump
        # fails, auto-merge never engages, and the ha-addon cascade stalls.
        #
        # hle-client's publish job already blocks until the version is
        # installable before dispatching here, and that is still not enough:
        # it checks from a self-hosted runner while this job runs on a
        # GitHub-hosted one, so the two ask different PyPI CDN edges. On
        # 2026-09-07 the publish check passed at 12:51 and this job was still
        # served an index without the version at 12:53, then spent its whole
        # five-minute budget on the same stale edge and gave up.
        #
        # So: no cache, because pip holds an index page for ~10 minutes and a
        # stale one here means every retry re-reads the same answer; and a
        # budget long enough to outlast an edge that has not caught up, rather
        # than one tuned to how long an upload takes.
        run: |
          for i in $(seq 1 40); do
            if pip install --no-cache-dir -r requirements.txt; then
              exit 0
            fi
            echo "pip install failed (attempt ${i}/40) — likely PyPI propagation lag; retrying in 30s..."
            sleep 30
          done
          echo "::error::pip install -r requirements.txt failed after 40 attempts (~20 minutes)"
          exit 1

      - name: Syntax check
        run: python -m compileall -q backend

      - name: Ruff (best-effort)
        run: |
          pip install ruff
          ruff check backend || echo "::warning::ruff reported issues"

  frontend:
    name: Frontend typecheck + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install
        run: npm ci

      - name: Typecheck
        run: npx tsc -b --noEmit

      - name: Build
        run: npm run build
